{
  "name": "my-calorie-doctor-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build && node scripts/create-static-routes.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
